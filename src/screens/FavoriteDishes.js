import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const FavoriteDishes = () => {
  return (
    <View>
      <Text>FavoriteDishes</Text>
    </View>
  );
};

export default FavoriteDishes;

const styles = StyleSheet.create({});
